{
  "name": "medinest-root",
  "version": "1.0.0",
  "description": "MediNest SaaS Platform - Root workspace",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix mnm-nextjs",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix mnm-nextjs\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
